services:
  - type: web
    name: calculatrice-ui
    runtime: static
    buildCommand: cd calculatrice-ui && npm install && npm run build
    staticPublishPath: calculatrice-ui/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html